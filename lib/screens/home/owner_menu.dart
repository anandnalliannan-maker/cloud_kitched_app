import 'package:flutter/material.dart';

class OwnerMenuScreen extends StatelessWidget {
  const OwnerMenuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Menu management coming next'),
    );
  }
}
